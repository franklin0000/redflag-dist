import React from 'react';

/**
 * PremiumGate — NO LONGER BLOCKS CONTENT.
 * The app is now free for everyone >18.
 * This component just passes through children.
 */
export default function PremiumGate({ children }) {
    return children;
}
