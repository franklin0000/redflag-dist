services:
  - type: web
    name: redflag-source
    runtime: node
    region: oregon
    plan: starter
    buildCommand: cd server && npm install --production
    startCommand: node server/index.js
    autoDeploy: true
